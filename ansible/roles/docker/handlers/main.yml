---
- name: Restart docker
  ansible.builtin.systemd_service:
    name: docker
    state: restarted

- name: Restart docker-user-chain
  ansible.builtin.systemd_service:
    name: docker-user-chain
    daemon_reload: true
    state: restarted
